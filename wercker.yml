build:
  box: node:latest
  steps:
    - internal/docker-build: 
        dockerfile: Dockerfile 
        image-name: test
    - internal/docker-push: 
        image:-name test
        username: $USERNAME
        password: $PASSWORD
        repository: $USERNAME/test
        tag: latest

build:
  box: node:latest
  steps:
    - internal/docker-build: 
        dockerfile: Dockerfile 
        image-name: $USERNAME/test
    - internal/docker-push: 
        image: $USERNAME/test
        username: $USERNAME
        password: $PASSWORD
        registry: https://hub.docker.com
        repository: $USERNAME/test
        tag: latest
